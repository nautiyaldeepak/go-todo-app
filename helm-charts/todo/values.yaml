kube-prometheus:
  enabled: true
# prometheus-operator-crds:
#   enabled: true

postgresql:
  enabled: true
  volumePermissions:
    enabled: true
  global:
    postgresql:
      auth:
        username: "test-user"
        password: "abc123d"
        database: "todos-db"

postgresqlEndpoint: todos-postgresql.default.svc.cluster.local