apiVersion: v1
kind: Service
metadata:
  name: todos-service
  annotations:
    prometheus.io/port: "metrics"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/part-of: todos-sm
spec:
  selector:
    app: todos
  ports:
  - name: app-port
    protocol: TCP
    port: 80
    targetPort: 3000
  - name: metrics
    protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP